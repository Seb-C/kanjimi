version: '3.8'

services:
  database:
    image: postgres:11
    restart: on-failure
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: test
    ports:
      - "5432:5432"
    volumes:
      - database:/var/lib/postgresql/data
  watch:
    image: node:14-alpine
    restart: on-failure
    working_dir: /kanjimi
    command: ./node_modules/.bin/tsc --watch -p ./src/Server/tsconfig.json
    volumes:
      - $PWD:/kanjimi
  server:
    image: node:14-alpine
    restart: on-failure
    working_dir: /kanjimi
    depends_on:
      - database
      - watch
    env_file:
      - docker-compose.env
    command: >
      sh -c "
        while [ ! -f ./dist/server/Server/server.js ]; do
          echo 'Waiting for the server source files...'
          sleep 1
        done;
        ./node_modules/.bin/nodemon -w ./server --expose-gc dist/server/Server/server.js
      "
    #command: ./node_modules/.bin/nodemon -w ./server --inspect=0.0.0.0:9229 --expose-gc dist/server/Server/server.js
    healthcheck:
      test: wget -q https://localhost:3000/api/health-check/ -O /dev/null --no-check-certificate
      interval: 15s
      timeout: 3s
      retries: 3
      start_period: 2m
    ports:
      - "3000:3000"
      #- "9229:9229"
    volumes:
      - $PWD:/kanjimi
      - mails:/tmp/mails
  migrate:
    image: node:14-alpine
    restart: on-failure
    working_dir: /kanjimi
    depends_on:
      - database
      - watch
    env_file:
      - docker-compose.env
    command: >
      sh -c "
        while [ ! -f ./dist/server/Server/migrate.js ]; do
          echo 'Waiting for the migrate source files...'
          sleep 1
        done;
        node dist/server/Server/migrate.js
      "
    volumes:
      - $PWD:/kanjimi
  extension:
    image: node:14-alpine
    restart: on-failure
    working_dir: /kanjimi
    env_file:
      - docker-compose.env
    command: >
      sh -c "
        ./node_modules/.bin/webpack --color --config=src/Extension/webpack.config.js --build;
        ./node_modules/.bin/webpack --color --config=src/Extension/webpack.config.js --watch
      "
    healthcheck:
      test: cat ./dist/extension/content.build.js
      interval: 3s
      timeout: 1s
      retries: 3
      start_period: 30s
    volumes:
      - $PWD:/kanjimi
  webapp:
    image: node:14-alpine
    restart: on-failure
    working_dir: /kanjimi
    env_file:
      - docker-compose.env
    command: >
      sh -c "
        ./node_modules/.bin/webpack --color --config=src/WebApp/webpack.config.js --build;
        ./node_modules/.bin/webpack --color --config=src/WebApp/webpack.config.js --watch
      "
    healthcheck:
      test: cat ./www/js/app.build.js
      interval: 3s
      timeout: 1s
      retries: 3
      start_period: 30s
    volumes:
      - $PWD:/kanjimi
  smtp:
    image: hmlio/spamsink
    restart: on-failure
    environment:
      - SINK_PORT=25
      - SINK_HOSTNAME=smtp
    volumes:
      - mails:/opt/spamsink/mails
volumes:
  database:
  mails:
