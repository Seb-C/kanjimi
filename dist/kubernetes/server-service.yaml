apiVersion: v1
kind: Service
namespace: kanjimi
metadata:
  labels:
    app: server-service
  name: server-service
spec:
  type: NodePort
  ports:
    - port: 443
      targetPort: 3443
  selector:
    app: server-deployment
