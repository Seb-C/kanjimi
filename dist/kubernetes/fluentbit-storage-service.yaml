apiVersion: v1
kind: Service
metadata:
  labels:
    app: fluentbit-storage-service
  name: fluentbit-storage-service
spec:
  type: ClusterIP
  ports:
    - port: 5170
      targetPort: 5170
  selector:
    app: fluentbit-storage-deployment
