apiVersion: v1
kind: ConfigMap
metadata:
  name: config-fluentbit-storage
data:
  fluent-bit.conf: |
    [INPUT]
        Name tcp
        Listen 0.0.0.0
        Port 5170
        Format none

    [OUTPUT]
        Name file
        Path /kanjimi/logs
        Format plain
